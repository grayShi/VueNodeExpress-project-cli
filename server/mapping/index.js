// const {
//   Blogs,
//   Tag,
//   User
// } = require('../model');
//
// // Blogs.belongsTo(Company, {foreignKey:'companyId'});
// Blogs.hasMany(Tag, {foreignKey: 'blogId', targetKey: 'id', as: 'blogTag'});
// Tag.belongsTo(Blogs, {foreignKey: 'blogId', as: 'tagBlog'});
